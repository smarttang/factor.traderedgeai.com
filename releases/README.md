# 发布文件目录

将已构建的 DLL 与 MQ4 包按版本放在此目录下，例如：

- `v1.0/TraderEdgAIFactor.dll` — 32 位 Windows DLL
- `v1.0/mql4-include.zip` — 包含 `Include/*.mqh` 与可选 `Experts/*.mq4` 的压缩包

首页下载链接会指向这些路径。若使用外部 CDN 或 GitHub Releases，请在 `website/index.html` 中将链接改为完整 URL。
